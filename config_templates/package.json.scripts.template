{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "serve": "npm run build && python3 -m http.server 5000 -d dist/public",
    "check": "tsc"
  }
}
