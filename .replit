run = ["bash", "-lc", "npm run dev -- --host 0.0.0.0 --port 5173"]
modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[env]
PORT = "5173"

[[ports]]
localPort = 5173
externalPort = 80

[[ports]]
localPort = 5174
externalPort = 3000

[deployment]
run = ["sh", "-c", "npm run build && npx vite preview --host 0.0.0.0 --port 80"]
build = ["sh", "-c", "npm run build"]
